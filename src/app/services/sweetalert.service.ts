import { Injectable } from '@angular/core';
// import { SweetAlert } from 'sweetalert/typings/core';
// import * as _swal from 'sweetalert';
// const swal: SweetAlert = _swal as any;

@Injectable()
export class SweetalertService {
	// message(message: string, title = '') {
	// 	swal(title, message);
	// }

	// success(message: string, title = ''): Promise<any> {
	// 	return swal(title, message, 'success');
	// }

	// error(message: string, title = ''): Promise<any> {
	// 	return swal(title, message, 'error');
	// }

	// info(message: string, title = ''): Promise<any> {
	// 	return swal(title, message, 'info');
	// }
}
